node_modules/
dist/
coverage/
*.log

# local Claude Code settings (personal overrides)
.claude/settings.local.json

# local, non-committed helper scripts
/push.cmd

# secrets — never commit
.env
.env.*
!.env.example
secrets/
*.pem
*.key
